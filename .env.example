# phoenixd limited-access password (from ~/.phoenix/phoenix.conf)
# Get it with: grep '^http-password-limited-access' ~/.phoenix/phoenix.conf | cut -d= -f2
PHOENIXD_PASSWORD=your-limited-access-password-here
